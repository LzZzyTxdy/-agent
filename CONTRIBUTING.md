# 贡献指南

感谢你考虑改进网页自动答题 Agent。

## 开发流程

1. Fork 仓库并从主分支创建功能分支。
2. 使用 Python 3.10 或更高版本创建独立虚拟环境。
3. 安装依赖：`pip install -r requirements.txt`。
4. 保持浏览器操作、语义求解和决策校验之间的模块边界。
5. 为行为变化补充测试，并运行 `pytest -q`。
6. 提交 Pull Request，说明问题、实现方式和验证结果。

## 提交要求

- 不得提交 `.env`、API Key、Cookie、账号信息或其他凭据。
- 不得提交真实答题记录、缓存、题库或未获授权的 PDF 资料。
- 不得移除或绕过最终提交保护。
- 站点特定 selector 应集中维护在 `browser/selectors.py`。
- 新功能应尽可能提供不启动浏览器即可执行的逻辑测试。

## 问题报告

报告 Bug 时，请提供可复现步骤、预期行为、实际行为、Python 版本和脱敏后的 DOM 片段。不要提供账号、密码、Cookie、Token、真实 API Key 或包含个人信息的页面内容。
