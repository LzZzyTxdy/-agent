# 网页自动答题 Agent 项目介绍

## 项目概述

网页自动答题 Agent 是一个基于 Python、Playwright、大语言模型（LLM）与检索增强生成（RAG）技术构建的智能答题系统，面向具有固定 DOM 结构的在线测试页面。

项目将浏览器自动化与语言模型能力解耦：Playwright 负责确定性地读取题目、识别题型、选择答案和切换页面；LLM 负责理解题意并生成候选答案；决策策略层负责校验答案格式、选项范围和置信度。这样的分层设计可以减少模型输出不稳定对浏览器操作的影响，提高整个答题流程的可靠性和可维护性。

## 技术架构

系统主要由以下模块组成：

- **浏览器控制层**：连接并控制浏览器，读取题目 DOM，操作单选框或复选框，并处理上一页、下一页及结束页面。
- **题目提取层**：从页面中提取题目 ID、题干、题型和选项，并转换为统一的数据模型。
- **智能求解层**：通过 OpenAI-compatible Chat Completions API 调用 LLM，获得结构化答案、置信度和判断理由。
- **检索增强层**：检索项目目录中的本地 PDF；本地资料不足时，可使用网页搜索补充参考信息。
- **决策策略层**：检查单选和多选答案的结构、索引范围、置信度及异常情况，并根据配置执行接受、重试、人工确认或停止。
- **缓存与记录层**：缓存高置信度答案、PDF 索引和网页搜索结果，并使用 JSONL 保存每次运行的逐题记录。

整体处理流程如下：

```text
读取网页题目
    ↓
检查答案缓存
    ↓ 未命中
检索本地 PDF ──→ 必要时搜索网页
    ↓
LLM 结合题目和参考资料生成答案
    ↓
决策策略校验答案与置信度
    ↓
选择页面选项并核对实际选中状态
    ↓
记录结果、进入下一题
    ↓
到达最终提交页后停止，等待人工检查
```

## 核心功能

### 单选题与多选题

系统能够根据页面中的 input 类型识别单选题和多选题。单选答案使用一个零基索引表示，多选答案使用索引数组表示。执行点击后，程序还会读取 radio 或 checkbox 的最终状态，确认页面实际选中的答案与决策一致。

### 本地 PDF 与网页检索

项目目录中的 PDF 会自动建立页级文本索引。每道题求解前，系统优先从本地资料中检索相关段落；当本地资料相关性不足时，可以调用有超时限制的公共网页搜索作为补充。

检索结果会与题目共同提供给 LLM。终端输出和运行日志会记录实际使用的 PDF 页码或网页地址，使答案具有可追踪的参考来源。PDF 文件发生变化时，本地索引会自动重建；网页搜索结果也会单独缓存，以减少重复请求和等待时间。

### 顺序无关的答案缓存

许多测试系统会在不同答题轮次中随机调整选项顺序。如果缓存的只是选项序号，同一答案在下一次测试中可能指向完全不同的内容。

本项目使用版本化的 v2 缓存结构，保存最终答案文本，而不是页面上的临时序号。缓存命中时，系统会校验题干、题型以及与排列顺序无关的选项集合指纹，然后将答案文本映射到本次页面的实际索引。因此，即使选项顺序发生变化，系统仍能选择相同的答案。题干或选项内容变化时，旧答案会自动失效并重新交给 LLM 求解，无需反复手动清理缓存。

### 多种运行模式

项目提供三种运行方式：

- **自动模式**：答案通过策略校验后，自动选择并进入下一题。
- **人工确认模式**：每题显示 AI 建议，用户可以接受、修改、跳过或停止。
- **只读调试模式**：只提取和展示当前页面信息，不调用 LLM，也不执行点击。

此外，低置信度答案可以根据配置进入人工确认、重新求解、直接接受或停止流程。

### 运行记录与故障保护

程序会逐题记录以下信息：

- 题目 ID、题干和全部选项；
- AI 决策与浏览器实际选中的答案；
- 答案置信度和判断理由；
- 使用的 PDF 页码或网页来源；
- 答案来自缓存还是 LLM；
- 当前题目的执行状态和异常信息。

记录采用逐行追加的 JSONL 格式。即使运行中途出现异常，已经完成的题目记录也不会丢失。

## 安全设计

系统不会自动处理账号密码、验证码、SSO 或二次验证，登录过程由用户手动完成。程序也不会点击“提交试卷”“交卷”等最终提交按钮。完成最后一题后，浏览器会保持打开，用户可以人工检查所有答案，并自行决定是否提交。

项目同时提供 DOM 调试模式，可在不调用 LLM、不改变页面答案的情况下检查题目提取和按钮识别结果，从而降低适配新页面时的误操作风险。

## 测试效果

项目已完成题目提取、LLM 求解、本地 PDF 检索、网页搜索、答案缓存、异常重试、运行记录和最终提交保护等主要功能。在当前测试环境中，加入 RAG 检索和顺序无关缓存后，多次测试成绩均稳定在 90 分以上。

当前结果说明：本地参考资料能够显著补充模型知识，而基于答案文本的缓存机制解决了随机选项顺序导致的历史答案错位问题。不过，实际准确率仍会受到题库内容、参考资料覆盖程度、网页搜索质量和所使用模型能力的影响。

## 项目特点

- Playwright 驱动的确定性 DOM 读取与浏览器操作；
- 支持单选题和多选题；
- 支持 OpenAI-compatible LLM API；
- 支持本地 PDF 页级检索和网页搜索；
- 提供带来源记录的 RAG 答题流程；
- 使用能够适应选项随机乱序的 v2 答案缓存；
- 支持自动、人工确认和只读调试模式；
- 提供置信度校验、失败重试与人工兜底；
- 保存完整的逐题运行记录；
- 保护最终提交操作，避免不可逆的自动交卷。

## 项目价值

本项目展示了浏览器自动化、结构化 LLM 输出和检索增强生成技术如何协同构成一个可实际运行的 Agent。相比直接让模型控制浏览器，项目通过明确的模块边界、数据模型、策略校验和执行后验证，将模型限制在语义判断环节，把确定性的页面操作交给自动化程序完成。

它可以作为 Python 浏览器自动化、LLM Agent、RAG 检索、结构化输出、缓存设计与自动化测试等方向的综合实践案例，也为其他固定流程网页 Agent 的开发提供可复用的设计思路。
